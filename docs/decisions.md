# 决策日志（decisions.md）

> 约定：涉及设计取舍的决定，先写进这里再动手。格式：日期 + 背景 + 选择 + 原因。

---

## 2026-08-19｜Go 切分微服务：独立部署

- **背景**：技术选型时决定用 Go 实现一个独立的文本切分微服务。
- **选择**：模块 2 用 Go 实现切分服务（`chunker/`），Python 通过 HTTP 调用。
- **原因**：Go 服务只做"纯文本切块"（无状态、约 100 行），PDF 文本提取等重活留在 Python 侧，保持轻量。交付时 Go 服务进 docker-compose。

## 2026-08-19｜版本控制策略：本地提交为主，GitHub 按里程碑推送

- **背景**：涉及"一次一功能、做完提交"的提交目标到底是本地仓库还是 GitHub。
- **选择**：本地仓库 D:\agenthub 是唯一提交点（每功能 commit 一次）；GitHub 按里程碑（每模块验收通过或每天结束时）推送。
- **原因**：本地 commit 已完整保留历史；GitHub 国内网络不稳定，每小步都推浪费时间为风险高；里程碑推送兼顾备份与展示。

## 2026-08-19｜会话双层存储：元数据落 MySQL、消息落 Redis

- **背景**：原设计只提到"会话历史存 Redis（TTL）"，没说会话列表/统计存哪。
- **选择**：MySQL 建 sessions 表只存元数据（session_id/agent_id/标题/条数/时间），消息内容存 Redis 并设 TTL。
- **原因**：Redis 天然适合"多轮上下文 + 过期"；但列表/统计/审计需要持久化元数据，MySQL 是本职。两层各取所长。

## 2026-08-19｜检索可插拔：use_rag 开关

- **背景**：架构图里编排层含"检索"，但模块 3 的验收只是"拿到大模型回答"，知识库/双路检索要到模块 2/6 才完成。
- **选择**：agents.use_rag 字段开关，模块 3 先纯 LLM 跑通；检索/工具随模块进度逐步接入，不做大重构。
- **原因**：每步都可验收、可回滚；编排通道从第一天就预留插槽，避免后期返工。

## 2026-08-19｜模块 2 入库先只写 Chroma，ES 索引到模块 6 再建

- **背景**：原计划把 ES 放模块 6，但没写明模块 2 入库时要不要同步建 ES 索引。
- **选择**：模块 2-5 只走向量单路；模块 6 才装 ES 并回填索引。
- **原因**：8GB 机器上 ES 是内存大户，提前白养一份没被使用的索引浪费资源；收敛范围，按功能模块交付。

## 2026-08-19｜不引入 Alembic 等重量级迁移工具

- **背景**：建表需要迁移工具。
- **选择**：开发期用 SQLAlchemy `create_all` 建表；确有结构调整时手写迁移脚本；不引入 Alembic。
- **原因**：单机开发、表只有 6 张，引入迁移框架是过度设计；体现范围收敛意识。

## 2026-08-19｜新增"原生 HTML+JS 控制台"前端（对原设计"无正式前端"的修订）

- **背景**：纯后端 + Swagger 只能点 JSON，用户反馈"真实交互下来才有意思，光后端感受不到每个功能"。原设计把"正式前端"列为明确不做。
- **选择**：新增一个**零依赖**的网页控制台（`web/`，vanilla HTML/CSS/JS + fetch 调 REST API），由 FastAPI 用 StaticFiles 直接托管（`/ui`）。不装 node、不引框架、不加内存。控制台随模块逐步生长：当前 Agent 管理页签 → 后续知识库/对话/日志页签。
- **原因**：满足"真实交互"诉求的同时守住 8GB 机器 + 不引入重框架的底线；手写 fetch 能完全掌控每一行代码。Streamlit 演示页保留在收尾阶段（截图/录 GIF 用），不与之冲突。

## 2026-08-26｜模块 5 设计决策汇总

- **背景**：实现工具调用（Function Calling）+ 调用日志，让 Agent 能"查资料/查时间/翻名言"再回答。
- **选择与原因**：
  1. **工具注册表模式**：每个工具 = ｛definition（给模型的说明书）+ execute（执行函数）｝一个字典注册。加新工具只需在 `app/tools/registry.py` 加一行。工具的"说明书"由模型决定是否调用，执行才是真干活，两者解耦。
  2. **Function Calling 循环**：max 4 轮（`MAX_TOOL_ROUNDS`）。模型请求调工具 → 执行 → 结果拼回 messages → 再问模型，最多 4 轮防止"反复调工具不回答"的死循环/烧钱。OpenAI 兼容协议：assistant 消息需回带完整 tool_calls，工具结果用 `role=tool` + `tool_call_id`。
  3. **工具执行依赖 ToolCallContext**：工具不是纯函数，知识库检索要知道"这个 Agent 绑了哪些库"——通过 context 传入 AgentOut。选择检索范围的语义：只搜 Agent 绑定库，不跨库。
  4. **先答后存 + 全量留证**：每调一次工具（含参数、结果、耗时、成败）写一行 `tool_call_logs`；结果超过 2000 字截断防撑爆。工具出错也记录、不让整轮对话崩。
  5. **测试确定性策略**：为了让"是否调用工具"不随模型发散，测试用 prompt 里写死"必须调用某工具"——测的是工具链路通不通，不是模型想不想用。

## 2026-08-26｜模块 4 设计决策汇总

- **背景**：实现多轮上下文（治"金鱼记忆"），涉及存储选型与兼容性取舍。
- **选择与原因**：
  1. **双层会话存储落地**：消息本体存 Redis List（key=`chat:history:{session_id}`，TTL 2 小时、每聊一轮续命）；MySQL sessions 表只存台账（谁开的/几句了/标题），供列表页展示。Redis 过期不影响台账。
  2. **滑窗拼接**：每次只取最近 5 轮（`lrange(key, -10, -1)` 一条命令搞定）拼进 messages。理由：历史越长 token 越贵越慢且可能超模型上限；固定窗口是成本与效果的最简平衡，"摘要压缩"留作后续优化。
  3. **先答后存**：本轮问答在模型成功返回后才写入历史。理由：调用失败时不留脏上下文。
  4. **RESP2 协议显式锁定**：redis-py 8 默认 RESP3 握手（HELLO 命令），但开发机 Redis 是 5.0（无 HELLO）报错；客户端 `protocol=2` 一行兼容新旧服务器。生产 docker-compose 用官方 Redis 7 镜像不受影响。
  5. **开发机 Redis 选型波折**：微软 Windows 版（3.0）→ 太老不兼容新客户端；Memurai 安装器 1603 失败；最终用 tporadowski Redis 5.0.14 绿色版 + 启动文件夹自启脚本（vbs 无黑窗）。教训：Windows 上跑中间件优先选"绿色解压版"，避开安装器的权限坑。
  6. **sessions.agent_id 可空外键（ondelete=SET NULL）**：删 Agent 时会话台账保留（历史不该消失），只是归属置空。

## 2026-08-20｜模块 2 设计决策汇总

- **背景**：一次实现知识库全链路（上传→切块→向量→入库→检索），涉及几处取舍。
- **选择与原因**：
  1. **同步处理而非异步**：文档上传后同步完成切块 + 向量化才返回。原因：模块 2 阶段文件小、同步能给实时反馈（状态立即从 processing → ready），且不引入后台任务框架（范围收敛）。异步留给后续优化，状态机字段已经预留（pending/processing/ready/failed）。
  2. **切块策略（Go 实现）**：段落 (\n\n)→行 (\n)→句子 (句末标点)→硬切，逐级自然边界断开；贪心装块（每块 ≤ chunk_size）；滑窗重叠（下块带上一块末尾 chunk_overlap 字符）。默认 500 字符/块、重叠 50。原因：贴合中文文档结构（"按意群断"），重叠保住跨块语义，这是经典的 RAG 切块策略。
  3. **Explicit embeddings（不配 embedding_function）**：向量显式由百炼 text-embedding-v4 生成并传给 Chroma，不让 Chroma 自己下载本地 embedding 模型。原因：统一用厂商模型、省本机内存、Embedding 质量与后续双路检索一致。
  4. **agent_kb_bindings.knowledge_base_id 不加物理外键**：完整性由应用层 `_validate_kb_ids` 校验（绑定前查知识库是否存在，否则 400）。原因：避免跨表物理外键在 create_all 建表顺序上的别扭，且 6 张表范围明确。
  5. **Chroma 持久化到 ./data/chroma**（已 gitignore）：文档正文/切块向量不进 MySQL，MySQL 只做台账。原因：各存储各司其职（见 database.md）。

## 2026-08-19｜Agent 服务层直接返回响应模型（AgentOut），而非 ORM 对象

- **背景**：模块 1 实现中发现，FastAPI 用 `from_attributes` 序列化 ORM 对象时，`knowledge_base_ids`（由绑定关系算出）不存在于 ORM 属性上，触发 ResponseValidationError。
- **选择**：service 层所有对外函数在 session 仍打开时就把 ORM 物化为 `AgentOut` 再返回；API 层保持薄壳。
- **原因**：避免"session 已关闭后再懒加载"的坑；返回类型清晰，后续模块（对话）复用也安全。

## 2026-08-19｜API Key 安全策略（两层防线）

- **背景**：项目要上 GitHub 公开展示，但百炼 API Key 一旦泄露会被盗刷。
- **选择**：真实 Key 只存在本地 `.env`（.gitignore 忽略）；仓库只提交 `.env.example` 占位；git 层配 pre-commit/pre-push 钩子拦截 `.env` 入库（含 `git add -f` 强行走走 case）；将来首次公开前在控制台轮换 Key。
- **原因**：密钥永不进版本历史；钩子兜底防手滑；轮换兜底防对话记录泄露。输送环境变量进 docker-compose 时用 `${DASHSCOPE_API_KEY}` 透传。

## 2026-08-28｜模块 6 双路检索：ES + Chroma 融合排序

- **背景**：单靠向量检索会"矮子里拔将军"——用户搜"红烧肉"，库里根本没有这个词，它也能硬凑出几条"语义相近"的段落。这不符合企业场景的"可控检索"需求。
- **选择**：
  1. 用 Elasticsearch 8.11 做关键词检索（第一路），Chroma 做向量检索（第二路）
  2. 融合排序权重：向量 0.6 + ES 0.4（语义为主，字面为辅）
  3. 字面验证：所有关键词都不存在 → 直接返回空，不硬凑
  4. ES 限 512MB 内存（-Xms512m -Xmx512m），开发期不常驻，用完即关
- **原因**：
  - 向量检索擅长"语义泛化"（搜"咋登录"命中"登录方法"），但缺乏"字面约束"
  - ES 的 BM25 算法是工业标准关键词检索，能补上"搜红烧肉就必须有这仨字"的能力
  - 融合排序让"既含关键词又语义相近"的结果排前面，符合用户直觉
  - 8GB 机器必须限 ES 内存，否则会被 JVM 吃光
- **实现细节**：
  - ES 索引按知识库隔离：`kb_{kb_id}_chunks`
  - 每个切块写入 `{doc_id, text, source, chunk_index}`
  - 删除文档时同步清 ES（delete_by_query）
  - 分词器：先用 ES 内置 standard（中文按字切），后续可换 IK
- **取舍**：
  - 没上复杂的归一化（BM25 和余弦分数范围不同），直接用原始分加权——简单可控
  - 没上一开始就接中文分词器（如 jieba/IK），先用 standard 跑通——范围收敛，后续可优化
